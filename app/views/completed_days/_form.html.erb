<%= form_for @completed_day do |f| %>
 
  <% if @completed_day.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@completed_day.errors.count, "error") %> prohibited
        this submission from being saved:
      </h2>
      <ul>
        <% @completed_day.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <div class="form-group">
    <%= f.label :date %><br>
    <%= f.text_field :date, class: "form-control" %>
  </div>
 
  <div class="form-group">
    <%= f.submit "Submit", class: "btn btn-default" %>
  </div>
 
<% end %>